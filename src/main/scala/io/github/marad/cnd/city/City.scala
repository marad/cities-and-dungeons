package io.github.marad.cnd.city

case class City(
                 gold: Int,
                 preparation: Int,
                 hero: Hero,
                 buildings: Seq[Building]
               )
